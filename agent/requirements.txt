# agent/requirements.txt — Python dependencies for KDE AI Agent
#
# Install with: pip install -r requirements.txt --break-system-packages

# LLM Providers
anthropic>=0.39.0         # Anthropic Claude API (streaming + tool use)
# openai>=1.55.0           # Optional: OpenAI API support (future)

# System Integration
dbus-python>=1.3.2        # D-Bus session bus for QML/KDE communication
PyGObject>=3.48.0         # GLib main loop for D-Bus service
watchdog>=5.0.0           # File system monitoring (context file changes)

# Code Intelligence
tree-sitter>=0.23.0       # AST parsing for repo_map() generation
gitpython>=3.1.43         # Git operations (auto-commit on file_write)

# Observability
# openobserve-sdk>=0.1.0   # Optional: OpenObserve SDK (uses raw HTTP fallback)

# Rich Terminal Output
rich>=13.0.0              # Terminal formatting for debug mode
